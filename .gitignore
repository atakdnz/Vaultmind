*.iml
.gradle
/local.properties
local.properties
/.idea
.DS_Store
/build
**/build/
/captures
.externalNativeBuild
.cxx

# Models (large binary files — sideload or download separately)
*.bin
*.gguf
*.tflite
*.task
*.litertlm

# Vault packages (user data — never commit)
*.rvault
